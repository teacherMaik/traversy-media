const posts = [
  {id: 1, title: 'Post one'},
  {id: 2, title: 'Post two'},
];

const getPosts = () => posts;

/* Export more than one function */
export { getPosts };

/* Export as default function */

export default getPosts;